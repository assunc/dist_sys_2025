package com.example.springsoap.Repository;

import com.example.springsoap.Entities.AirlineSupplier;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AirlineSupplierRepository extends JpaRepository<AirlineSupplier, Integer> {
}